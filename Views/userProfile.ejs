<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>User Profile</title>
	<!-- Bootstrap CSS -->
	<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
	<!-- Font Awesome Icons -->
	<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
	<!-- Custom CSS -->
	<link rel="stylesheet" href="/css/userprofile.css">
	<style>
		body {
			background: linear-gradient(135deg, #6a11cb 0%, #2575fc 100%);
			font-family: 'Roboto', sans-serif;
			height: 100vh;
			margin: 0;
			display: flex;
			justify-content: center;
			align-items: center;
		}

		.profile-card {
			background: linear-gradient(135deg, #ffffff, #f1f1f1);
			border-radius: 15px;
			box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
			width: 400px;
			text-align: center;
			padding: 40px 30px;
			transition: 0.3s;
		}

		.profile-card:hover {
			box-shadow: 0 12px 24px rgba(0, 0, 0, 0.2);
		}

		.profile-avatar {
			height: 120px;
			width: 120px;
			border-radius: 50%;
			margin: 0 auto 20px auto;
			overflow: hidden;
			border: 3px solid #007bff;
		}

		.set {
			object-fit: cover;
			background-position: center;
			height: 120px;
			width: 120px;
		}

		.profile-card h2 {
			font-size: 1.8rem;
			font-weight: bold;
			margin-bottom: 10px;
		}

		.profile-card p {
			font-size: 1.1rem;
			margin-bottom: 5px;
			color: #6c757d;
		}

		.profile-card .icon {
			font-size: 1.2rem;
			color: #6c757d;
			margin-right: 10px;
		}

		.profile-info {
			text-align: left;
			margin-top: 20px;
		}

		.profile-info p {
			margin-bottom: 15px;
			display: flex;
			align-items: center;
		}

		.profile-actions {
			display: flex;
			justify-content: space-between;
			margin-top: 30px;
		}

		.profile-actions .btn {
			padding: 10px 20px;
			font-size: 1rem;
			border-radius: 25px;
			transition: 0.3s;
			flex: 1;
			margin: 0 5px;
		}

		.btn-back {
			background-color: #007bff;
			color: white;
			text-decoration: none;
		}

		.btn-back:hover {
			background-color: #0056b3;
			color: white;
		}

		.btn-logout {
			background-color: #dc3545;
			color: white;
			text-decoration: none;
		}

		.btn-logout:hover {
			background-color: #c82333;
			color: white;
		}

		/* Responsive Adjustments */
		@media (max-width: 576px) {
			.profile-card {
				width: 90%;
				padding: 30px 20px;
			}

			.profile-actions {
				flex-direction: column;
				align-items: center;
			}

			.profile-actions .btn {
				width: 100%;
				margin: 5px 0;
			}
		}
	</style>
</head>

<body>
	<div class="profile-card">
		<div class="profile-avatar">
			<img src="https://upload.wikimedia.org/wikipedia/commons/9/99/Sample_User_Icon.png" alt="User Avatar" class="set">
		</div>
		<h2 class="mt-3"><%= userDetails.name %></h2>
		<p class="text-muted">User</p>

		<div class="profile-info">
			<p><i class="fas fa-envelope icon"></i> <%= userDetails.email %></p>
			<p><i class="fas fa-phone icon"></i> <%= userDetails.phone %></p>
			<p>
				<i class="fas fa-lock icon"></i> ********
				<a href="/change-password" class="edit-link ms-2 text-decoration-none">Change Password</a>
			</p>
		</div>

		<div class="profile-actions">
			<a href="/" class="btn btn-back">Dashboard</a>
			<a href="/logout" class="btn btn-logout">Logout</a>
		</div>
	</div>

	<!-- Bootstrap JS -->
	<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
